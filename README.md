# calcu-teorema-role

ejecutar:

```bash
node ./index.js "pow(x, 2)" 2 -2
```

el primer parametro es la funcion, en este caso $x^2$, el segundo es el punto $a$ (en este caso $2$) y el tercero $b$ (en este caso $-2$)